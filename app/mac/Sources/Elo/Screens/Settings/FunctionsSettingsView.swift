import SwiftUI

/// The "Functions" settings tab. Empty for now.
struct FunctionsSettingsView: View {
    var body: some View {
        Text("Functions")
            .foregroundStyle(.secondary)
            .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}
