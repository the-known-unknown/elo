import SwiftUI

/// The "Application" settings tab. Empty for now.
struct ApplicationSettingsView: View {
    var body: some View {
        Text("Application")
            .foregroundStyle(.secondary)
            .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}
