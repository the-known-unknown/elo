import SwiftUI

/// The "AI Models" settings tab. Empty for now.
struct AIModelsSettingsView: View {
    var body: some View {
        Text("AI Models")
            .foregroundStyle(.secondary)
            .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}
